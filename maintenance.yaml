openapi: "3.0.3"
info:
  title: Sanshain Maintenance API
  description: |
    The administrative surface of Sanshain: users, roles, groups, maintainers,
    Producer and Consumer administration, version deletion, settings and
    observability.

    Kept separate from `api.yaml` deliberately. That file is the contract build
    plugins and CLIs are written against and must stay stable; this one changes
    whenever the admin interface does. One document would imply one stability
    promise for both.

    **Authorisation.** Every endpoint states what it requires. `Any signed-in
    caller` marks the read-only listings the dashboard needs before it knows who
    is looking; everything else names a permission. Producer-scoped endpoints
    additionally admit a maintainer of the Producer named in the path. An account
    listed in `SANSHAIN_ROOT_USERS` holds every permission.

    See [Administration](docs/administration.md) for the concepts.
  version: "2.2.0"
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.txt

servers:
  - url: http://localhost:3000
    description: Local development

security:
  - bearerAuth: []

paths:
  /admin/auth-config:
    get:
      summary: "Read: auth-config"
      description: |
        Requires the `manage_auth_config` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    put:
      summary: "Replace: auth-config"
      description: |
        Requires the `manage_auth_config` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/auth-config/test:
    post:
      summary: "Create or run: auth-config/test"
      description: |
        Requires the `manage_auth_config` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/cache/clear:
    post:
      summary: "Create or run: cache/clear"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/cleanup/dependencies:
    post:
      summary: "Create or run: cleanup/dependencies"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/cleanup/snapshots:
    post:
      summary: "Create or run: cleanup/snapshots"
      description: |
        Requires the `manage_settings` permission.

        Runs the use-based snapshot expiry immediately: snapshots neither provided nor required within `snapshot_max_age_days` are deleted. GA versions are never age-culled.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/consumers:
    get:
      summary: "Read: consumers"
      description: |
        Any signed-in caller.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/consumers/{name}:
    delete:
      summary: "Delete: consumers/name"
      description: |
        Requires the `manage_consumers` permission.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/consumers/{name}/endpoints:
    get:
      summary: "Read: consumers/name/endpoints"
      description: |
        Any signed-in caller.

        Every endpoint the Consumer is recorded as depending on, with the pinned version and its current stability.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/endpoint-versions:
    get:
      summary: "Read: endpoint-versions"
      description: |
        Any signed-in caller.

        The blame trail of one endpoint: which versions of the line changed it, with the Author attribution recorded on each Provide.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/endpoint-yaml:
    get:
      summary: "Read: endpoint-yaml"
      description: |
        Any signed-in caller.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/groups:
    get:
      summary: "Read: groups"
      description: |
        Requires the `manage_roles` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: groups"
      description: |
        Requires the `manage_roles` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/groups/{id}:
    put:
      summary: "Replace: groups/id"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    delete:
      summary: "Delete: groups/id"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/groups/{id}/members:
    post:
      summary: "Create or run: groups/id/members"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/groups/{id}/members/{user_id}:
    delete:
      summary: "Delete: groups/id/members/user_id"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: user_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/maintainers:
    get:
      summary: "Read: maintainers"
      description: |
        Requires the `manage_roles` permission.

        Every Producer with its maintainer user and group ids, in one response — the aggregate behind the dashboard's Maintainers section, so it does not ask per Producer.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/nuke/consumers:
    post:
      summary: "Create or run: nuke/consumers"
      description: |
        Requires the `run_destructive_operations` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/nuke/database:
    post:
      summary: "Create or run: nuke/database"
      description: |
        Requires the `run_destructive_operations` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/nuke/producers:
    post:
      summary: "Create or run: nuke/producers"
      description: |
        Requires the `run_destructive_operations` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/nuke/users:
    post:
      summary: "Create or run: nuke/users"
      description: |
        Deletes every user who is not an effective administrator — users with a
        direct `admin` role, `admin` via group membership, or a configured root
        username survive. Requires the `run_destructive_operations` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/observability/audit-logs:
    get:
      summary: "Read: observability/audit-logs"
      description: |
        Requires the `view_audit` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/observability/audit-logs/export:
    get:
      summary: "Read: observability/audit-logs/export"
      description: |
        Requires the `view_audit` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/observability/debug-config:
    get:
      summary: "Read: observability/debug-config"
      description: |
        Any signed-in caller.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/observability/debug-config-update:
    post:
      summary: "Create or run: observability/debug-config-update"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/observability/logs:
    get:
      summary: "Read: observability/logs"
      description: |
        Any signed-in caller.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/observability/stats:
    get:
      summary: "Read: observability/stats"
      description: |
        Any signed-in caller.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers:
    get:
      summary: "Read: producers"
      description: |
        Any signed-in caller.

        Every Producer with metadata and its full version lines (version, stability, endpoint count, snapshot expiry).
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/metadata:
    post:
      summary: "Create or run: producers/metadata"
      description: |
        Requires the `manage_producers` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}:
    delete:
      summary: "Delete: producers/name"
      description: |
        Requires the `manage_producers` permission, **or** maintainership of the Producer named in the path.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/diff:
    get:
      summary: "Read: producers/name/diff"
      description: |
        Any signed-in caller.

        Unified diff between two versions of a line (`api_type`, `from`, `to` query parameters).
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/endpoints:
    get:
      summary: "Read: producers/name/endpoints"
      description: |
        Any signed-in caller.

        The endpoints of one version-line entry (`api_type`, `version` query parameters).
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/full-spec:
    get:
      summary: "Read: producers/name/full-spec"
      description: |
        Any signed-in caller.

        The full provided document of one version-line entry, exactly as the Producer submitted it (`api_type`, `version` query parameters).
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/maintainers:
    get:
      summary: "Read: producers/name/maintainers"
      description: |
        Requires the `manage_producers` permission, **or** maintainership of the Producer named in the path.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: producers/name/maintainers"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/maintainers/groups/{group_id}:
    delete:
      summary: "Delete: producers/name/maintainers/groups/group_id"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
        - name: group_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/maintainers/users/{user_id}:
    delete:
      summary: "Delete: producers/name/maintainers/users/user_id"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
        - name: user_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/versions:
    get:
      summary: "Read: producers/name/versions"
      description: |
        Any signed-in caller.

        One Producer's version lines, optionally narrowed with an `api_type` query parameter.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/versions/{api_type}/{version}:
    delete:
      summary: "Delete: producers/name/versions/api_type/version"
      description: |
        Requires the `manage_producers` permission, **or** maintainership of the Producer named in the path.

        The sole escape hatch from GA immutability (ADR-0003): delete the version outright, freeing its number. Consumers pinned to it hard-fail on their next require — check the dependents listing first. Audited, naming the pinned Consumers.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
        - name: api_type
          in: path
          required: true
          schema:
            type: string
        - name: version
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/versions/{api_type}/{version}/promote:
    post:
      summary: "Write: producers/name/versions/api_type/version/promote"
      description: |
        Requires the `release_ga` permission — enforced by the same GA gate every release goes
        through, so an unauthorized attempt answers the instructive `403` and is recorded as a
        `VERSION_REJECTED` audit entry with reason `ga_requires_releaser`.

        One-click promote: releases the stored snapshot in place, exactly as if its content were
        re-provided with `stability: ga` — the original provider stays credited, the promoting
        Actor is named in the `VERSION_PROMOTED` audit entry, and AsyncAPI GA contract checks
        apply. Promoting an already-GA version is an idempotent no-op (`promoted: false`).
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
        - name: api_type
          in: path
          required: true
          schema:
            type: string
        - name: version
          in: path
          required: true
          schema:
            type: string
      responses:
        "202":
          description: Promoted (or already GA — idempotent no-op). Standard Provide response body.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without the `release_ga` permission.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "404":
          description: Unknown Producer or version.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "409":
          description: |
            Rejected by the same rules a GA Provide faces — semver honesty
            against the GA baseline (carries `proposed_version`), an AsyncAPI
            channel-message contract owned by another Producer, or the snapshot
            changing between click and release.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/versions/{api_type}/{version}/dependents:
    get:
      summary: "Read: producers/name/versions/api_type/version/dependents"
      description: |
        Any signed-in caller.

        The Consumers currently pinned to this version — what a delete would break. Shown by the UI before a delete-version is confirmed.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
        - name: api_type
          in: path
          required: true
          schema:
            type: string
        - name: version
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/roles:
    get:
      summary: "Read: roles"
      description: |
        Requires the `manage_roles` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/settings/auto-approve:
    get:
      summary: "Read: settings/auto-approve"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: settings/auto-approve"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/settings/cache:
    get:
      summary: "Read: settings/cache"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: settings/cache"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/settings/database:
    get:
      summary: "Read: settings/database"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/settings/dependency-max-age:
    get:
      summary: "Read: settings/dependency-max-age"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: settings/dependency-max-age"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/settings/snapshot-max-age:
    get:
      summary: "Read: settings/snapshot-max-age"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: settings/snapshot-max-age"
      description: |
        Requires the `manage_settings` permission.

        Days a snapshot survives without being provided or required. `0` disables the cleanup.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/users:
    get:
      summary: "Read: users"
      description: |
        Requires the `manage_users` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/users/{id}:
    delete:
      summary: "Delete: users/id"
      description: |
        Requires the `manage_users` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/users/{id}/approve:
    post:
      summary: "Create or run: users/id/approve"
      description: |
        Requires the `manage_users` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/users/{id}/maintains:
    get:
      summary: "Read: users/id/maintains"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/users/{id}/roles:
    get:
      summary: "Read: users/id/roles"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: users/id/roles"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/users/{id}/roles/{role}:
    delete:
      summary: "Delete: users/id/roles/role"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: role
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer

  schemas:
    ErrorBody:
      type: object
      description: |
        The body every error response carries. Identical to the schema of the
        same name in `api.yaml`.
      required:
        - error
      properties:
        error:
          type: string
        proposed_version:
          type: string

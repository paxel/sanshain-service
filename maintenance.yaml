openapi: "3.0.3"
info:
  title: Sanshain Maintenance API
  description: |
    The administrative surface of Sanshain: users, roles, groups, maintainers,
    Producer and Consumer administration, settings, observability and the review
    of held Provides.

    Kept separate from `api.yaml` deliberately. That file is the contract build
    plugins and CLIs are written against and must stay stable; this one changes
    whenever the admin interface does. One document would imply one stability
    promise for both.

    **Authorisation.** Every endpoint states what it requires. `Any signed-in
    caller` marks the read-only listings the dashboard needs before it knows who
    is looking; everything else names a permission. Producer-scoped endpoints
    additionally admit a maintainer of the Producer named in the path. An account
    listed in `SANSHAIN_ROOT_USERS` holds every permission.

    See [Administration](docs/administration.md) for the concepts.
  version: "1.7.0"
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.txt

servers:
  - url: http://localhost:3000
    description: Local development

security:
  - bearerAuth: []

paths:
  /admin/auth-config:
    get:
      summary: "Read: auth-config"
      description: |
        Requires the `manage_auth_config` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    put:
      summary: "Replace: auth-config"
      description: |
        Requires the `manage_auth_config` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/auth-config/test:
    post:
      summary: "Create or run: auth-config/test"
      description: |
        Requires the `manage_auth_config` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/cache/clear:
    post:
      summary: "Create or run: cache/clear"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/cleanup/branches:
    post:
      summary: "Create or run: cleanup/branches"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/cleanup/dependencies:
    post:
      summary: "Create or run: cleanup/dependencies"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/consumers:
    get:
      summary: "Read: consumers"
      description: |
        Any signed-in caller.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/consumers/{name}:
    delete:
      summary: "Delete: consumers/name"
      description: |
        Requires the `manage_consumers` permission.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/consumers/{name}/branches:
    get:
      summary: "Read: consumers/name/branches"
      description: |
        Any signed-in caller.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/consumers/{name}/branches/{branch}/endpoints:
    get:
      summary: "Read: consumers/name/branches/branch/endpoints"
      description: |
        Any signed-in caller.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
        - name: branch
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/endpoint-versions:
    get:
      summary: "Read: endpoint-versions"
      description: |
        Any signed-in caller.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/endpoint-yaml:
    get:
      summary: "Read: endpoint-yaml"
      description: |
        Any signed-in caller.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/endpoints/update:
    post:
      summary: "Create or run: endpoints/update"
      description: |
        Requires the `manage_producers` permission, **or** maintainership of the Producer
        named in the request body. The Producer is not in the path, so the scoped check
        runs in the handler.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/groups:
    get:
      summary: "Read: groups"
      description: |
        Requires the `manage_roles` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: groups"
      description: |
        Requires the `manage_roles` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/groups/{id}:
    put:
      summary: "Replace: groups/id"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    delete:
      summary: "Delete: groups/id"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/groups/{id}/members:
    post:
      summary: "Create or run: groups/id/members"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/groups/{id}/members/{user_id}:
    delete:
      summary: "Delete: groups/id/members/user_id"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: user_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/maintainers:
    get:
      summary: "Read: maintainers (all Producers)"
      description: |
        Requires the `manage_roles` permission.

        Every Producer with its maintainer user and group ids, in one response —
        the aggregate behind the dashboard's Maintainers section, so it does not
        ask per Producer.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/nuke/branch/{branch}:
    post:
      summary: "Create or run: nuke/branch/branch"
      description: |
        Requires the `run_destructive_operations` permission.
      parameters:
        - name: branch
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/nuke/consumers:
    post:
      summary: "Create or run: nuke/consumers"
      description: |
        Requires the `run_destructive_operations` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/nuke/database:
    post:
      summary: "Create or run: nuke/database"
      description: |
        Requires the `run_destructive_operations` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/nuke/producers:
    post:
      summary: "Create or run: nuke/producers"
      description: |
        Requires the `run_destructive_operations` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/nuke/users:
    post:
      summary: "Create or run: nuke/users"
      description: |
        Requires the `run_destructive_operations` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/observability/audit-logs:
    get:
      summary: "Read: observability/audit-logs"
      description: |
        Requires the `view_audit` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/observability/audit-logs/export:
    get:
      summary: "Read: observability/audit-logs/export"
      description: |
        Requires the `view_audit` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/observability/debug-config:
    get:
      summary: "Read: observability/debug-config"
      description: |
        Any signed-in caller.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/observability/debug-config-update:
    post:
      summary: "Create or run: observability/debug-config-update"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/observability/logs:
    get:
      summary: "Read: observability/logs"
      description: |
        Any signed-in caller.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/observability/stats:
    get:
      summary: "Read: observability/stats"
      description: |
        Any signed-in caller.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/pending-specs:
    get:
      summary: "Read: pending-specs"
      description: |
        Any signed-in caller.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/pending-specs/{id}:
    get:
      summary: "Read: pending-specs/id"
      description: |
        Any signed-in caller.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    delete:
      summary: "Delete: pending-specs/id"
      description: |
        Any signed-in caller.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/pending-specs/{id}/accept:
    post:
      summary: "Create or run: pending-specs/id/accept"
      description: |
        Any signed-in caller.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers:
    get:
      summary: "Read: producers"
      description: |
        Any signed-in caller.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/metadata:
    post:
      summary: "Create or run: producers/metadata"
      description: |
        Requires the `manage_producers` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/onboarding:
    get:
      summary: "Read: producers/onboarding"
      description: |
        Any signed-in caller.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}:
    delete:
      summary: "Delete: producers/name"
      description: |
        Requires the `manage_producers` permission, **or** maintainership of the Producer named in the path.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/branches:
    get:
      summary: "Read: producers/name/branches"
      description: |
        Any signed-in caller.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/branches/{branch}:
    delete:
      summary: "Delete: producers/name/branches/branch"
      description: |
        Requires the `manage_producers` permission, **or** maintainership of the Producer named in the path.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
        - name: branch
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/branches/{branch}/endpoints:
    get:
      summary: "Read: producers/name/branches/branch/endpoints"
      description: |
        Any signed-in caller.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
        - name: branch
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/branches/{branch}/full-spec:
    get:
      summary: "Read: producers/name/branches/branch/full-spec"
      description: |
        Any signed-in caller.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
        - name: branch
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/branches/{branch}/reset-history:
    post:
      summary: "Create or run: producers/name/branches/branch/reset-history"
      description: |
        Requires the `manage_producers` permission, **or** maintainership of the Producer named in the path.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
        - name: branch
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/branches/{branch}/source-protected-branch:
    get:
      summary: "Read: producers/name/branches/branch/source-protected-branch"
      description: |
        Requires the `manage_producers` permission, **or** maintainership of the Producer named in the path.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
        - name: branch
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    put:
      summary: "Replace: producers/name/branches/branch/source-protected-branch"
      description: |
        Requires the `manage_producers` permission, **or** maintainership of the Producer named in the path.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
        - name: branch
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/maintainers:
    get:
      summary: "Read: producers/name/maintainers"
      description: |
        Requires the `manage_producers` permission, **or** maintainership of the Producer
        named in the path.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: producers/name/maintainers"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/maintainers/groups/{group_id}:
    delete:
      summary: "Delete: producers/name/maintainers/groups/group_id"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
        - name: group_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/maintainers/users/{user_id}:
    delete:
      summary: "Delete: producers/name/maintainers/users/user_id"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
        - name: user_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/producers/{name}/onboarding:
    get:
      summary: "Read: producers/name/onboarding"
      description: |
        Requires the `set_onboarding` permission, **or** maintainership of the Producer named in the path.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    put:
      summary: "Replace: producers/name/onboarding"
      description: |
        Requires the `set_onboarding` permission, **or** maintainership of the Producer named in the path.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/protected-branches:
    get:
      summary: "Read: protected-branches"
      description: |
        Requires the `manage_protected_branches` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: protected-branches"
      description: |
        Requires the `manage_protected_branches` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/protected-branches/{pattern}:
    delete:
      summary: "Delete: protected-branches/pattern"
      description: |
        Requires the `manage_protected_branches` permission.
      parameters:
        - name: pattern
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/roles:
    get:
      summary: "Read: roles"
      description: |
        Requires the `manage_roles` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/settings/auto-approve:
    get:
      summary: "Read: settings/auto-approve"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: settings/auto-approve"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/settings/branch-cleanup:
    post:
      summary: "Create or run: settings/branch-cleanup"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/settings/branch-max-age:
    get:
      summary: "Read: settings/branch-max-age"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: settings/branch-max-age"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/settings/cache:
    get:
      summary: "Read: settings/cache"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: settings/cache"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/settings/database:
    get:
      summary: "Read: settings/database"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/settings/dependency-cleanup:
    post:
      summary: "Create or run: settings/dependency-cleanup"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/settings/dependency-max-age:
    get:
      summary: "Read: settings/dependency-max-age"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: settings/dependency-max-age"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/settings/dev-mode:
    get:
      summary: "Read: settings/dev-mode"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: settings/dev-mode"
      description: |
        Requires the `manage_settings` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/users:
    get:
      summary: "Read: users"
      description: |
        Requires the `manage_users` permission.
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/users/{id}:
    delete:
      summary: "Delete: users/id"
      description: |
        Requires the `manage_users` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/users/{id}/approve:
    post:
      summary: "Create or run: users/id/approve"
      description: |
        Requires the `manage_users` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/users/{id}/maintains:
    get:
      summary: "Read: users/id/maintains"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/users/{id}/roles:
    get:
      summary: "Read: users/id/roles"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    post:
      summary: "Create or run: users/id/roles"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
  /admin/users/{id}/roles/{role}:
    delete:
      summary: "Delete: users/id/roles/role"
      description: |
        Requires the `manage_roles` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: role
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "401":
          description: Not signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "403":
          description: Signed in, but without what this endpoint requires.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer

  schemas:
    ErrorBody:
      type: object
      description: |
        The body every error response carries. Identical to the schema of the
        same name in `api.yaml`.
      required:
        - error
      properties:
        error:
          type: string
        pending_id:
          type: integer
          format: int64
        status:
          type: string

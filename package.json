{
  "private": true,
  "type": "module",
  "scripts": {
    "fmt": "cargo fmt",
    "fmt:check": "cargo fmt-check",
    "lint": "cargo lint",
    "test:unit": "cargo test",
    "test:itest": "cargo itest",
    "test:ui": "cargo ui-test",
    "test": "cargo test",
    "check": "npm run fmt:check && npm run lint && npm run test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^10.0.0",
    "globals": "^16.0.0",
    "prettier": "^3.0.0"
  }
}

image: alpine/latest
packages:
  - sqlite-dev
  - rust
  - cargo
  - openssl-dev
  - build-base
sources:
  - https://git.sr.ht/~taum/sanshain
tasks:
  - build: |
      cd SanshainService
      cargo build --release
  - test: |
      cd SanshainService
      cargo test
